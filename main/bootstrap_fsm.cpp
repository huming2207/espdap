#include <esp_event.h>
#include "bootstrap_fsm.hpp"

esp_err_t bootstrap_fsm::decode_message(const char *topic, size_t topic_len, uint8_t *buf, size_t buf_len)
{
    // Verify message first

    return 0;
}
